import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'golf-mastery',
  webDir: '../../dist/apps/golf-mastery',
  bundledWebRuntime: false,
};

export default config;
